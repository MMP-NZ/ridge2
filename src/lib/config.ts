/**
 * "Ridge" is a codename — the product name is on hold until the Juno Logic
 * trade mark is cleared (CLAUDE.md). Never put "Ridge" in customer-facing
 * text; import PRODUCT_NAME instead so the real name can be dropped in
 * later by changing exactly one value.
 */
export const PRODUCT_NAME = process.env.NEXT_PUBLIC_PRODUCT_NAME ?? "Juno Logic Platform";
